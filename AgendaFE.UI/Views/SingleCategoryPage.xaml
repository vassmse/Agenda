<Page
    x:Class="AgendaFE.UI.Views.SingleCategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AgendaFE.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Black" BorderThickness="2" Grid.Row="0" Margin="10" >
            <Grid Grid.Row="0" Height="80">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <CheckBox Grid.Row="0" Grid.RowSpan="2" Margin="30"/>

                <TextBlock Text="Banana" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" >
                    <TextBlock Text="Fruit" />
                    <TextBlock Text="2018-02-16" Padding="20 0" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.RowSpan="2" Grid.Column="2" >
                    <SymbolIcon  Symbol="GlobalNavigationButton" Margin="10 0"/>
                    <SymbolIcon  Symbol="Bookmarks" Margin="10 0"/>
                    <SymbolIcon  Symbol="Home" Margin="10 0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border BorderBrush="Black" BorderThickness="2" Grid.Row="1" Margin="10" >
            <Grid Grid.Row="0" Height="80">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <CheckBox Grid.Row="0" Grid.RowSpan="2" Margin="30"/>

                <TextBlock Text="Banana" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBlock Text="Fruit" />
                    <TextBlock Text="2018-02-16" Padding="20 0" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.RowSpan="2" Grid.Column="2" >
                    <SymbolIcon  Symbol="GlobalNavigationButton" Margin="10 0"/>
                    <SymbolIcon  Symbol="Bookmarks" Margin="10 0"/>
                    <SymbolIcon  Symbol="Home" Margin="10 0"/>
                </StackPanel>
            </Grid>
        </Border>


    </Grid>
</Page>
